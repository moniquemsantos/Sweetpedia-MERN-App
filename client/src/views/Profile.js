function Profile() {
  // Build fetch function to fetch user profile data
  //Inside fetch first we get the token from the localstorage, and we insert it in the headers of the fetch.

  //NEXT STEPS : build a server end point to post recipies ...and correspondinf client component...

  return (
    <div>
      <p>UserProfile</p>
    </div>
  );
}

export default Profile;
