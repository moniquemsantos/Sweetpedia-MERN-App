import { styled } from "@mui/material/styles";
import { Paper } from "@mui/material";

export const PaperProfile = styled(Paper)(() => ({
  elevation: "24",
  padding: 20,
  width: "90%",
  height: "fit-content",
  margin: "20px auto",
}));
